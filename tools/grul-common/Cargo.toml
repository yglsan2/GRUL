[package]
name = "grul-common"
description = "Shared types and profile loading for GRUL tools"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
